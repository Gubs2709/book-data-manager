
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow all authenticated users read/write access.
    // This is a temporary rule for development to fix permission errors.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
